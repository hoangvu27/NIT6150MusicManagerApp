<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
<!--        <item name="windowActionBar">false</item>-->
<!--        <item name="windowNoTitle">true</item>-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Customize your LoginActivity style -->
    <style name="LoginTheme" parent="AppTheme">
        <!-- Customize your theme here -->
    </style>

    <!-- Customize your RegisterActivity style -->
    <style name="RegisterTheme" parent="AppTheme">
        <!-- Customize your theme here -->
    </style>

    <!-- Customize your MusicManagementActivity style -->
    <style name="MusicManagementTheme" parent="AppTheme">
        <!-- Customize your theme here -->
    </style>

</resources>